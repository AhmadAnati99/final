<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Delete Flight</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 20px;
        }
        .container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            padding: 30px;
        }
        h1 {
            color: #333;
            text-align: center;
            margin-bottom: 30px;
        }
        .btn-group {
            text-align: center;
            margin-top: 20px;
        }
        .btn {
            background-color: #f44336;
            color: white;
            padding: 12px 20px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s ease;
        }
        .btn:hover {
            background-color: #d32f2f;
        }
    </style>
</head>
<body>
    <h1>Delete Flight</h1>
    <div class="container">
        <form id="deleteForm" action="/delete-flight/<%= flight.id %>?_method=DELETE" method="POST">
            <p>Are you sure you want to delete flight ID <%= flight.id %>?</p>
            <div class="btn-group">
                <button type="button" onclick="confirmDelete()" class="btn">Delete</button>
                <a href="/lastpart" class="btn">Cancel</a>
            </div>
        </form>
    </div>

    <script>
        function confirmDelete() {
            var confirmed = confirm("Are you sure you want to delete this flight?");
            if (confirmed) {
                document.getElementById('deleteForm').submit();
            }
        }
    </script>
</body>
</html>
