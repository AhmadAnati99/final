<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Airplane Information</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
      margin: 0;
      padding: 0;
    }

    .container {
      max-width: 800px;
      margin: 0 auto;
      padding: 20px;
      background-color: #fff;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      text-align: center;
    }

    h1 {
      color: #333;
    }

    .plane-image {
      max-width: 100%;
      height: auto;
    }

    .button {
      display: inline-block;
      padding: 10px 20px;
      background-color: #007bff;
      color: #fff;
      text-decoration: none;
      border-radius: 4px;
      transition: background-color 0.3s ease;
      margin: 10px;
    }

    .button:hover {
      background-color: #0056b3;
    }

    .user-info {
      position: absolute;
      top: 10px;
      right: 10px;
    }

    .logout-button {
      background-color: #dc3545;
      padding: 8px 16px;
      margin-left: 10px;
    }

    .logout-button:hover {
      background-color: #bd2130;
    }
  </style>
</head>
<body>
  <div class="user-info">
    <% if (loggedIn) { %>
      Logged in as <%= user.user_name %>
      <form action="/logout" method="GET">
        <button type="submit" class="logout-button">Logout</button>
      </form>
    <% } %>
  </div>
  
  <div class="container">
    <h1>Airplane Information</h1>
    <img src="plane2.jpg" alt="Airplane" class="plane-image">
    <p>Here you can head to the Airplanes section</p>
    <a href="Airplane" class="button">Explore Airplanes (Functions)</a>
    <a href="flightslist.html" class="button">View Flight List</a>
    <a href="lastpart" class="button">View Last Part</a>
  </div>
</body>
</html>
